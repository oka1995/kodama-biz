{
  "name": "ana-availability-checker",
  "version": "0.1.0",
  "private": true,
  "description": "ANA国際線 空席待ち人数 自動照会ツール",
  "type": "module",
  "scripts": {
    "start": "node src/check.js",
    "check": "node src/check.js",
    "postinstall": "playwright install chromium"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  }
}
